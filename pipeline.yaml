#DroneCI!
kind: pipeline
type: ssh
name: default
server:
  host: 13.126.193.83
  user: centos
  password:
    from_secret: ssh_password
steps:
- name: build
  commands:
    - cd myapp
    - /opt/maven/bin/mvn clean install
    - cd ..
    - sudo docker build -t sampleimage .  
    - docker images
    - docker push haripreethi.jfrog.io/default-docker-local/mydocker:new
    
#- name: publish  
#  image: sampleimage
 # settings:
  #  username:
   #   from_secret: docker_username
   # password:
     # from_secret: docker_password
   # repo: haripreethisagar/new
   # auto_tag: true
   # auto_tag_suffix: drone-build
      
  






      
   
    
    
   
